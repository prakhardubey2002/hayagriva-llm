# AGENT.md

## Purpose

This file gives coding agents a reliable, repo-specific operating manual for **hayagriva-llm**.

Hayagriva-llm is a CLI tool that generates structured LLM metadata files (llm.package.json and llm.package.txt) for Node.js packages. It analyzes package manifests and source code to create comprehensive AI-readable descriptions of package functionality, helping AI systems better understand and interact with npm packages. The tool integrates seamlessly with existing package.json workflows and supports TypeScript analysis for accurate type information.

## Quickstart

```
npm install
npm test
npm run lint
npm run build
```

## Common commands

- **Generate LLM metadata**:

```
npx hayagriva-llm generate
# or
hayagriva-llm generate
```

- **Start local dashboard**:

```
npx hayagriva-llm dashboard
# or
hayagriva-llm dashboard --port 4177
```

- **Generate this file**:

```
npx hayagriva-llm agent
# or
hayagriva-llm agent
```

## Project snapshot

- **name**: hayagriva-llm
- **version**: 2.0.0
- **module type**: module
- **main**: dist/cli.cjs
- **module**: dist/cli.mjs
- **source**: src/cli.ts

## Package behavior (from llm.package.json)

- **mode used to generate llm metadata**:
  - ai
- **frameworks**:
- node

- **hooks**:
- (none)

- **side effects**:
- reads package.json
- analyzes source code
- writes llm.package.json and llm.package.txt files

- **keywords**:
- llm
- metadata
- npm
- package
- llm.txt
- cli
- typescript
- ai
- documentation
- package-management
- code-analysis
- ai-assistants
- automated-documentation
- hayagriva
- node
- command-line
- static-analysis
- ai-analysis
- dashboard
- observability

## API surface (exports)

- `cli` — Starts the Hayagriva CLI, parsing command-line arguments and dispatching to the appropriate command handler. It initializes the application state, loads configuration from environment variables, and runs the selected operation such as generating LLM metadata or starting the observability dashboard.

## Repo map (expected)

- **`src/`**: CLI + generation logic
- **`test/`**: Vitest tests
- **`website/`**: Docusaurus docs
- **`dist/`**: build output (published)

## Agent workflow

- **Understand intent**: read `README.md`, then relevant source under `src/`.
- **Prefer small, safe changes**: keep public CLI behavior stable.
- **Follow existing patterns**: ESM, `commander`, `ts-morph` for static mode, OpenRouter only in AI mode.
- **After edits**: run lint/tests/build locally if you are allowed in your environment.

## Conventions

- **Node**: 18+
- **Module system**: ESM (`"type": "module"`)
- **CLI**: `src/cli.ts` with `commander`
- **Generated artifacts**: `llm.package.json`, `llm.package.txt`, optional `.cursor/rules/*.mdc`

## Release & publishing notes

- `npm publish` runs lint/tests/build via `prepublishOnly`.
- Only `dist/` is published (see `files` in `package.json`).

## Troubleshooting

- **AI mode auth errors**: set `OPEN_ROUTER_API_KEY` (or pass `--api-key`).
- **No exports found**: ensure your `package.json` entry/exports points to the right file.
- **Rule generation**: pass `--rule` to `generate` to write `.cursor/rules/<pkg>.mdc`.

---

*Generated by hayagriva-llm@3.0.0 on 2026-03-31T06:39:37.744Z.*
